# 🧹 清空配置

::: warning
清空配置前务必关闭程序。
:::

## Linux

```sh
rm -rf ~/.mdc
```

## macOS

### 方法一

1. 打开 Finder
2. 点击顶栏`前往`
3. 点击`个人`进入家目录
4. 按下快捷键`cmd + shift + .`显示隐藏文件夹和文件
5. 删除`.mdc`文件夹

### 方法二

打开终端，运行：

```sh
rm -rf ~/.mdc
```

## Windows

打开资源管理器，删除 `C:/Users/用户名` 下的 `.mdc` 文件夹。
